# Pawhub Demo

Vite + React + Tailwind clickable demo generated from Wonder page code.

## Local run
```bash
npm install
npm run dev
```

## Vercel
Import this folder/repo as a Vite project. Build command: `npm run build`. Output directory: `dist`.
